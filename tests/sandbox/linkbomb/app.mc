// The innocent half of the hostile project: an ordinary program that compiles
// in a fraction of a second. What is hostile is mc.toml, which names the
// "linker" that runs after this file is compiled to an object.
i64 main() {
    return 0;
}
