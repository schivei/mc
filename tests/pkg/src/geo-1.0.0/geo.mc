// geo.mc -- geo 1.0.0. Every value it answers differs from 1.2.0's, so a build
// that picked this tree would not merely differ: it would fail its own test.
#include "vec.mc"

i64 geo_dot(i64 ax, i64 ay, i64 bx, i64 by) { return 0; }
i64 geo_version() { return 100; }
